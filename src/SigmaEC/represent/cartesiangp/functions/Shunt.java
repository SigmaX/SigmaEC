package SigmaEC.represent.cartesiangp.functions;

/**
 * Eric 'Siggy' Scott */
public class Shunt implements BooleanFunction {
    @Override public boolean[] execute(boolean[] inputs) {
        return inputs;
    }
}
